import './styles.css';
import Loader from 'shared/ui/Loader';

const PageLoader = () => {
  return (
    <div className={'loader_container'}>
      <Loader/>
    </div>
  );
};

export default PageLoader;
