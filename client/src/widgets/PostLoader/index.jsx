import './styles.css';
import Loader from 'shared/ui/Loader';

const PostLoader = () => {
  return (
    <div className={'post_loader'}>
      <Loader/>
    </div>
  );
};

export default PostLoader;
